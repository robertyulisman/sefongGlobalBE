"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var EncodingMode;
(function (EncodingMode) {
    /** Uses full ASCII range interpreted as Latin-1 or binary data. **/
    EncodingMode[EncodingMode["DATA_MODE"] = 0] = "DATA_MODE";
    /** Uses pre-formatted UTF-8 input. */
    EncodingMode[EncodingMode["UNICODE_MODE"] = 1] = "UNICODE_MODE";
    /** Encodes GS1 data using FNC1 characters. */
    EncodingMode[EncodingMode["GS1_MODE"] = 2] = "GS1_MODE";
    /** Process input data for escape sequences. */
    EncodingMode[EncodingMode["ESCAPE_MODE"] = 8] = "ESCAPE_MODE";
})(EncodingMode || (EncodingMode = {}));
exports.default = EncodingMode;
